{% extends 'HezarfenDenetmenBundle:Template:layout.html.twig' %}
{% block body %}
    <table>
        <thead>
        <tr>
            <td>Url</td>
            <td>Route</td>
            <td>Status</td>
            <td>Response Time</td>
            <td>Exception</td>
        </tr>
        </thead>
        <tbody>
        {% for error in errors %}
            <tr>
                <td>{{ error.url|striptags }}</td>
                <td>{{ error.routeKey|striptags }}</td>
                <td>{{ error.statusCode|striptags }}</td>
                <td>{{ error.responseTime|striptags }}</td>
                <td>{{ error.exception|striptags }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
